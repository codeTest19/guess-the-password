const appConfig = {
  endpoints: {
    validatePassword: "http://localhost:4000/api/validate-password",
    newPassword: "http://localhost:4000/api/new-password"
  }
};

export default appConfig;
